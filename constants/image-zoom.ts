/**
 * Constants for image zoom functionality
 */
export const ZOOM_CONSTANTS = {
  MIN_SCALE: 0.5,
  MAX_SCALE: 5,
  ZOOM_STEP: 1.2,
  DEFAULT_SCALE: 1,
} as const

export const TOUCH_CONSTANTS = {
  MIN_TOUCH_DISTANCE: 0,
  PINCH_TOUCH_COUNT: 2,
  SINGLE_TOUCH_COUNT: 1,
} as const

